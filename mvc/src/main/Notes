when we request through localhost:8081 it downloads home.jsp file
It will not render jsp files So we convert them as Servlets To Render
the data.
We use Tomcat Jasper For that and added that dependency
